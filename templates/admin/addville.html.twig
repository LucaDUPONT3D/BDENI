{% extends 'base.html.twig' %}

{% block body %}
    <h3> Gérer les villes</h3>
    <h4>Filtrer les villes</h4>
{% include('bodyStructures/forms/villeForm.html.twig') %}
    <table>
        <tr>
            <th>Ville</th>
            <th>Code Postal</th>
            <th>Action</th>
        </tr>
        {%for ville in tableauVille %}
        <tr>
            <td>{{ ville.nom }}</td>
            <td>{{ ville.codePostal }}</td>
            <td><a href="{{ path('admin_delete_ville',{'id' : ville.id})}} "><button class="btn btn-danger">Supprimer</button></a>
                <a href="{{ path('admin_update_ville',{'id' : ville.id})}} "><button class="btn btn-primary">Modifier</button></a></td>
        </tr>
        {% endfor %}
        <tr>
            {{ form_start(addville) }}

            <td> {{ form_row(addville.nom) }}</td>
            <td>{{ form_row(addville.codePostal) }}</td>
            <td> <button type="submit" class="btn btn-primary" > Enregistrer</button></td>
            {{ form_end(addville) }}
        </tr>

    </table>

{% endblock %}

{% block title %}

{% endblock %}

