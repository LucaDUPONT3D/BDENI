{% extends 'base.html.twig' %}

{% block title %}

{% endblock %}

{% block body %}
    {{ form_start(updateVille) }}
    {{ form_row(updateVille) }}
    <button type="submit" class="btn btn-primary" > Enregistrer</button>
    {{ form_end(updateVille) }}

{% endblock %}

